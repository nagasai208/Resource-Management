import styled from '@emotion/styled'
import tw from 'tailwind.macro'
const SelectTag = styled.select`
   ${tw`w-24 `}
`

const SelectMainDIv = styled.div`
   ${tw`mt-2 flex justify-center`}
`

export { SelectTag, SelectMainDIv }
