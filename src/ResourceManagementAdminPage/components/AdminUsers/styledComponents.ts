import styled from '@emotion/styled'
import tw from 'tailwind.macro'

const AllUsersMainDiv = styled.div`
   ${tw` min-h-screen `}
`

const SreachDiv = styled.div`
   ${tw`mt-24 m-32 border`}
`

export { AllUsersMainDiv, SreachDiv }
