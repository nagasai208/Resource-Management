import styled from '@emotion/styled'
import tw from 'tailwind.macro'

const AdminResourcesMainDiv = styled.div`
   ${tw` min-h-screen `}
`
const SreachDiv = styled.div`
${tw`mt-24 ml-32`}`

export { AdminResourcesMainDiv,SreachDiv }
